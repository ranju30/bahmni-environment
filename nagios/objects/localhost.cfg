###############################################################################
###############################################################################
#
# HOST DEFINITION
#
###############################################################################
###############################################################################

# Define a host for the local machine

define host{
    use                     linux-server    ; Name of host template to use
                                            ; This host definition will inherit all variables that are defined
                                            ; in (or inherited by) the linux-server host template definition.
    host_name               localhost
    alias                   localhost
    address                 127.0.0.1
}


define host{
    use                     linux-server    ; Name of host template to use
                                            ; This host definition will inherit all variables that are defined
                                            ; in (or inherited by) the linux-server host template definition.
    host_name               jssemr001
    alias                   jssemr001
    address                 192.168.0.152
}

define host{
    use                     linux-server    ; Name of host template to use
                                            ; This host definition will inherit all variables that are defined
                                            ; in (or inherited by) the linux-server host template definition.
    host_name               jssemr002
    alias                   jssemr002
    address                 192.168.0.115
}

###############################################################################
###############################################################################
#
# HOST GROUP DEFINITION
#
###############################################################################
###############################################################################

# Define an optional hostgroup for Linux machines

define hostgroup{
    hostgroup_name  linux-servers ; The name of the hostgroup
    alias           Linux Servers ; Long name of the group
    members         localhost     ; Comma separated list of hosts that belong to this group
}



###############################################################################
###############################################################################
#
# SERVICE DEFINITIONS
#
###############################################################################
###############################################################################

###################### OS ############################
# Define a service to check the disk space of the root partition
# on the local machine.  Warning if < 20% free, critical if
# < 10% free space on partition.

define service{
        use                             rdc1service         ; Name of service template to use
        host_name                       grameen001
        service_description             Root Partition
        check_command                   check_nrpe!check_disk
        }


# Define a service to check the number of currently running procs
# on the local machine.  Warning if > 250 processes, critical if
# > 400 users.

define service{
        use                             rdc1service         ; Name of service template to use
        host_name                       grameen001
        service_description             Total Processes
        check_command                   check_nrpe!check_procs
        }

# Define a service to check the load on the local machine. 

define service{
        use                             rdc1service         ; Name of service template to use
        host_name                       grameen001
        service_description             Current Load
        check_command                   check_nrpe!check_load
        }



# Define a service to check the swap usage the local machine. 
# Critical if less than 10% of swap is free, warning if less than 20% is free

define service{
        use                             rdc1service         ; Name of service template to use
        host_name                       grameen001
        service_description             Swap Usage
        check_command           check_nrpe!check_swap
        }


######################## SERVICES ########################
# Define a service to check SSH on the local machine.
# Disable notifications for this service by default, as not all users may have SSH enabled.

define service{
        use                             rdc1service         ; Name of service template to use
        host_name                       grameen001
        service_description             SSH
    check_command           check_ssh
    notifications_enabled       0
        }



# Define a service to check HTTP on the local machine.
# Disable notifications for this service by default, as not all users may have HTTP enabled.

#define service{
#       use                             rdc1service         ; Name of service template to use
#       host_name                       localhost
#       service_description             HTTP
#       check_command                   check_http
#       notifications_enabled           0
#        }

#TomcatRDC1
define service{
        use                             rdc1highlyavailableservice         ; Name of service template to use
        host_name                       grameen001
        service_description             TOMCAT
        check_command           check_http_tomcat
    }

#TomcatRDC2
define service{
        use                             rdc2highlyavailableservice         ; Name of service template to use
        host_name                       localhost
        service_description             TOMCAT
        check_command                   check_http_tomcat
        }

#postgresOnRDC1
define service{
        use                             rdc1service         ; Name of service template to use
        host_name                       grameen001
        service_description             POSTGRES_RDC1
        check_command                   check_pgsql!postgres!ananya
        }

#postgresOnRDC2
define service{
        use                             rdc2service         ; Name of service template to use
        host_name                       localhost
        service_description             POSTGRES_RDC2
        check_command                   check_pgsql!postgres!ananya
    }

#apacheRDC1
define service{
        use                             rdc1highlyavailableservice         ; Name of service template to use
        host_name                       grameen001
        service_description             HTTPD
        check_command                   check_apache_httpd
    }

#apacheRDC2
define service{
        use                             rdc2highlyavailableservice         ; Name of service template to use
        host_name                       localhost
        service_description             HTTPD
        check_command                   check_apache_httpd
    }

#check jasperserver accessibility
define service {
        use                             rdc2service
        host_name                       localhost
        service_description             Webapp-Jasperserver
        check_command                   check_jasperserver
        normal_check_interval           60
        }

################# REPLICATION #####################
#postgresReplication
define service{
         use                             rdc2service
         host_name                       localhost
         service_description             Postgres-Ananya-Replication
         check_command                   check_postgres_replication!$HOSTADDRESS:grameen001$!postgres!ananya!10000!20000
        }

