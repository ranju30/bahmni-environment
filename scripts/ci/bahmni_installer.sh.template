#!/bin/bash -e
TEMP_SCRIPT_DIR=`dirname -- "$0"`
SCRIPT_DIR=`cd $TEMP_SCRIPT_DIR; pwd`
export SCRIPT_DIR

IMPLEMENTATION_CONFIG_FILE=$1

if [ "${IMPLEMENTATION_CONFIG_FILE}a" != "a" ]
then
	cp -f $IMPLEMENTATION_CONFIG_FILE /packages/build
fi

if [ $SCRIPT_DIR != "/packages/build" ]
then
        mv $SCRIPT_DIR/* /packages/build
fi

/bin/sh /packages/build/bahmni-environment/scripts/{{RUN_DEPLOY_SCRIPT}}